#!/bin/sh
set -eu

ORIGIN=$(dirname "$(readlink -f "$0")")
CMD=$(sh "$ORIGIN/bigdftmk" "$@")

if [ -z "$CMD" ]; then
    echo "ERROR: empty command generated by bigdftmk" >&2
    exit 1
fi

echo "$CMD"
exec sh -lc "$CMD"
